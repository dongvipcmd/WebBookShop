server:
  port: 8080
  servlet:
    context-path:
spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/book_management"
    username: root
    password: 123456ty
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true